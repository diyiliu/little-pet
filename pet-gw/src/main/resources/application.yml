# 接口测试页面 swagger-ui.html
# SPRING
spring:
  profiles:
    active: dev

  #json
  jackson:
    locale: zh_CN
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

# LOGGING
logging:
  config: 'classpath:config/logback.xml'